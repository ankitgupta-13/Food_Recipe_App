import { Categories, FilterSidebar, Header, SearchBar } from "../../components";

const Home = () => {
  return (
    <div>
      <Header />
      <SearchBar />
      <Categories />
      <FilterSidebar />
    </div>
  );
};

export default Home;
